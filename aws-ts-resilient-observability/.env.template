# Multi-Region Deployment Configuration
# Copy this file to .env and fill in your actual values

# Note: Account configuration is now handled directly in deployment-config.json
# No environment variables needed for basic deployment!

# Optional: Cross-account deployment role ARNs
# Only required if deploying to separate AWS accounts
# SHARED_SERVICES_ROLE_ARN=arn:aws:iam::123456789012:role/PulumiDeploymentRole
# WORKLOADS_ROLE_ARN=arn:aws:iam::987654321098:role/PulumiDeploymentRole

# Optional: Override default AWS profile if not using default
# AWS_PROFILE=your-profile-name

# Optional: Override default AWS region if not specified in config
# AWS_DEFAULT_REGION=us-east-1

# Optional: Override default regions
# PRIMARY_REGION=us-east-1
# SECONDARY_REGION=us-west-2

# Optional: Override default domain for Route53
# ROUTE53_DOMAIN=workloads.example.com

# Optional: Override default CIDR blocks
# SHARED_HUB_VPC_PRIMARY_CIDR=10.0.0.0/16
# SHARED_HUB_VPC_SECONDARY_CIDR=10.2.0.0/16
# WORKLOADS_SPOKE_VPC_PRIMARY_CIDR=10.1.0.0/16
# WORKLOADS_SPOKE_VPC_SECONDARY_CIDR=10.3.0.0/16